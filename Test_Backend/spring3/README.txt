2025년 6월 24일

※ 클라이언트에서 보낸 데이터가 여러 개일 경우 한꺼번에 모델로 받기

1) 작업 : html의 submit 버튼을 이용해서 전송
   - VO(DTO) 클래스 생성 
     (기본 생성자, setter(c -> s)가 있어야 한다.
     (getter (S -> C)
   - 콘트롤러에서는 전달인자 부분에 @ModelAttribute를 사용해야 한다. 
   
2) 작업 : 자바스크립트로 데이터 검증 후 전송
   - submit 버튼 -> button 버튼으로 수정
   - 일반 버튼을 자바스크립트로 실행
   - form 태그에 id를 지정한 후 그 아이디 값을 자바스크립트에서 읽어온다.
   - 자바스크립트의 submit() 메소드를 이용해 서버로 데이터 전송